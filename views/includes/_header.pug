header.header
  if showNav !== false
    a(href="#")
    img.logo(src="/img/logo.png" alt="Pickle ball logo")
    if userRole !== null
      case userRole
        when 'clubAdmin'
          include _navLoggedInAsAdmin
        when 'user'
          include _navLoggedInAsUser
        default
          // optionally nothing