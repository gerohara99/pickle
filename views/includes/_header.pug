header.header
  a.logo(href="/")
    img(src="/img/logo.png", alt="Pickle Logo")
  if showNav !== false
    if userRole !== null
      case userRole
        when 'clubAdmin'
          include _navLoggedInAsAdmin
        when 'user'
          include _navLoggedInAsUser
        default
          // optionally nothing